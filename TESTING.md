# Testing Strategy

Tests are organised in three layers. All tests live in `tests/` in this repo; no external test repos or live Canvas instances are required for the normal test suite.

## Running the tests

```sh
uv run pytest -q
```

1398 tests, all passing, in well under a minute (~25s). The full IMSCC import pipeline (Pandoc subprocesses included) used to run once per assertion in `test_imscc_import.py`; it now runs once per test module via a module-scoped `imported_dir` fixture, and the ~125 read-only tests just assert against the shared output. Tests that need their own isolated output directory (failure-mode tests, the zip-input test, tests that mutate the fixture) still call `run_import` directly.

## Layer 1 — Pure unit tests

Test individual functions in isolation, with no network or Canvas dependency. Each test passes in a string or small data structure and asserts on the output:

- **Snippet preprocessor** (`test_convert.py`): given Markdown text with `[text](../snippets/...)` links, assert the correct substitution; test nested-include error behaviour.
- **Link rewriter** (`test_link_rewrite.py`): given an HTML fragment and a manifest dict, assert that `<img src>` and `<a href>` are rewritten to the correct Canvas URLs; test each link type (page, assignment, discussion, asset, quiz, external, anchor).
- **Manifest** (`test_manifest.py`): TOML round-trips, flush-on-every-write behaviour, create-vs-update lookup logic, `needs_sync` timestamp comparisons.
- **Ignore files** (`test_ignore.py`): `load_ignore_matcher`/`IgnoreMatcher` — empty (no ignore files) matches nothing, `.gitignore` pattern matching (globs, `~$*`, directory-only `build/`), `.canvasignore` layering on top of `.gitignore`, git-style `!` negation, and the always-ignored manifests (`.manifest-canvas.toml`, a second config's `.manifest-canvas-sec-a.toml`, and the legacy `.canvas-manifest.toml`). Integration coverage in `test_sync.py` asserts an ignored stray asset isn't uploaded (and is when no ignore file is present) and an ignored content file isn't synced.
- **Config** (`test_config.py`): `config.load()` — base_url/course_id/token precedence (env var over `[auth]`), trailing-slash stripping, missing-key and missing-file errors, frozen dataclass, the recorded `config_path`, and the optional `[course_flags]` table (absent, read, non-boolean and invalid-name errors, and `require_course=False` tolerating a flags-only canvas.toml for `publish`/`list-titles`). Plus `_resolve_repo`.
- **Course flags** (`test_conditionals.py`): directive evaluation, `load_course_flags` over `course_settings.toml`, and the `canvas.toml` merge — union of the two tables, canvas.toml winning per flag on conflict, validation errors naming the offending file, and the printed `Flags:`/override lines. Integration: two configs over one repo producing different page bodies through `run_sync`, `flags_used` recorded per section in that section's own manifest, and `publish.stage()` honoring the override.
- **Quiz parsing** (`test_quiz.py`): quiz-level file parsing (frontmatter, question order, description), MCQ/essay/true-false question file parsing, answer weight assignment, correct-answer detection. New question types: `multiple_response_question` (correct as list, multiple weights 100), `fill_in_blank_question` (mapped to `short_answer_question`, all answers weight 100), `pattern_match_question` (first pattern becomes sole answer). Feedback: `## Feedback` section with `### General/Correct/Incorrect` subsections → `neutral_comments`, `correct_comments`, `incorrect_comments` in returned dict. Essay `## Sample Solution` → `neutral_comments`. No feedback section → no comment keys in dict.
- **Publish staging** (`test_publish.py`): unit tests for the `publish` subcommand's pure helpers — `staged_path` (quiz-folder flattening), `load_site_name` (course_settings.toml vs. dir-name fallback), `build_nav` (module → nav tree, SubHeader nesting, ExternalUrl items, referenced-content set), `stage_content_markdown` (frontmatter stripped, snippet expansion, quiz-link rewrite, H1 prepend), `render_quiz_study_guide` (correct-answer marking), `render_module_overview`, and `render_mkdocs_yml` (YAML round-trip). `stage()` is tested for the full staging tree and for excluding unreferenced content. `run_publish` is tested with `mkdocs` mocked (`subprocess.run` patched) for both `build` and `gh-deploy`, plus the missing-`mkdocs` error path. No MkDocs install is required for the test suite.
- **Local orphan detection** (`test_local_orphans.py`): unit tests for `collect_local_refs` per source type (content body links, subfolder links, external/anchor links excluded, `annotatable_attachment` frontmatter, links carried in via a snippet, malformed frontmatter, module items, module ExternalUrl items, quiz description and question links, question-bank question links, assets referencing nothing, and conditionals deliberately *not* applied); `collect_settings_refs` (`front_page`/`dashboard_image`, `pinned_resources` returned separately, `due_dates` contributing nothing, missing settings file); `collect_candidates` (snippets/modules/course_settings/question_banks/quizzes/announcements never candidates, `.canvasignore` pruning), announcement links still protecting assets; and `find_local_orphans` end to end over a full synthetic repo, plus pinned-folder suppression, unused snippets never reported, a clean repo, and the two degradation paths (a module item outside the repo, and a Pandoc conversion failure via monkeypatch) warning rather than crashing or inventing orphans. The `-v` report is covered too: `find_local_orphans` returns a `LocalOrphanReport`, with tests for per-candidate referrer lists, settings-derived references credited to `course_settings.toml`, pins labelled `(pinned_resources)`, and the invariant that `referenced` and `orphans` partition the candidate set while non-candidates appear in neither; `print_report` is asserted via `capsys` for referenced-before-unreferenced ordering, the section being omitted without `-v`, and the no-orphans case. The error path is covered too: a module item outside the repo, a Pandoc failure, and a conversion timeout each produce one `(key, message)` entry (the timeout asserted both with a monkeypatched `markdown_to_html` and end to end against a real pandoc with the module-level timeout lowered to 1s), plus `capsys` assertions that the error section prints *after* the findings, prints when there are no orphans, and is absent when everything scanned. All offline — no Canvas mock needed.
- **Nested span collapsing** (`test_imscc_convert.py`): `_collapse_redundant_spans` — a fully redundant nest is removed, the real Canvas shape (9 opens / 7 closes / `{#id}` / 2 closes) collapses to one span keeping its id, and everything that must survive does: a lone prose bracket, a lone `[text]{#id}`, links, reference links, task lists, escaped brackets, inline code (`a[i][j]`), fenced code, and unbalanced runs. Plus idempotency, a run spanning a line break, and a timing assertion that a collapsed run converts through Pandoc in under 5s (the pass exists because nested runs backtrack exponentially).
- **Bounded conversion** (`test_convert.py`): `markdown_to_html(text, timeout=...)` — the pypandoc path and the direct-subprocess path return identical HTML for a representative document (guards against flag drift between them), a 14-deep bracket run raises `subprocess.TimeoutExpired`, and the default call takes no timeout.
- **IMSCC parsing** (`test_imscc_convert.py`): unit tests for each XML parser — `_parse_manifest_metadata`, `_parse_course_settings_full`, `_parse_grading_standards`, `_parse_assignment_groups`, `_parse_late_policy`, `_parse_context`, `_parse_events`, `_parse_rubrics`, `_parse_files_meta`; HTML body extraction; frontmatter rendering; `parse_qti_questions` with both Canvas-extended format (`question_type` labels) and IMS CC format (`cc_profile` labels).
- **Centralized due dates** (`test_due_dates.py`): unit tests for `load_due_dates` (TOML inline-table parsing), `find_due_date_override` (name/type matching), `_extract_date_fields`, `_collect_due_date`, `format_due_dates_toml` (round-trip through TOML parser), `_build_frontmatter` with commented fields; integration tests for due-date override in `_sync_content_file` (assignments and discussions); import pipeline generating `due_dates` table and commenting out dates in `.md` files; `list-titles` CLI subcommand output, sorting, centralized-date display, and empty-repo edge case.
- **Repo format version** (`test_repo_format.py`, `test_repo_format_enforcement.py`): `read_repo_version`/`read_manifest_versions` (missing file or key = 0, invalid values name the file), `fix_tab_configuration` (nested under a table, nested inside an array-of-tables element, in both places, correctly placed, absent; comments elsewhere survive byte for byte), `check_repo_format` (older/newer repo, older/newer manifest named in the message; it compares versions only and leaves a nested `tab_configuration` untouched, as do `run_sync` and `run_mv`), the manifest's `_repo_format` stamp (new manifest stamped on first flush; `has_content_entries` ignores it), and `run_upgrade` with a fake migration registry (a failing second step leaves `format_version` at the first step's result; a second upgrade appends a second `upgraded_by` entry; a manifest-only upgrade adds none; `--noop` writes nothing). The enforcement file runs every checked entry point (`run_sync`, `run_targeted_sync`, `run_prune`, `check_course`, `run_mv`, `run_publish`, `find_local_orphans`, `run_plan`, `load_manifest`, `collect_title_items`) on a version-0 repo and on a current repo with an unstamped manifest and asserts each raises `RepoFormatError` before any file is written or any Canvas call is made; per-command tests that the reserved `_repo_format` entry is neither pruned, rewritten by `mv`, nor counted as content; `CliRunner` tests that every repo-reading command exits non-zero with the `upgrade` message and no traceback while `import`, `emit-workflow` and `setup` never call the check; the `upgrade` command (normal, `--noop`, newer-than-tool refusal, repo found from the cwd); and an end-to-end test that imports the IMSCC fixture, makes it look like an old repo (no version keys, nested `tab_configuration`, legacy manifest), upgrades it and runs `update --check-all`.
- **Processing order**: asset-first, module-last, alphabetical sorting of folders and files within folders.
- **Frontmatter parsing**: all content types and their specific fields.

## Layer 2 — Integration tests with mocked Canvas (`test_sync.py`)

Mock the `canvasapi` library with `pytest-mock`. Run the full sync pipeline against the fixture course and assert on which `canvasapi` methods were called, with what arguments, and in what order. Key scenarios to cover:

- First sync (no manifest): all items created, manifest written after each item
- Second sync (manifest present): all items updated, no creates called
- Stub creation: an assignment that links to a page not yet synced triggers a stub create before the assignment upload, followed by a real page upload that overwrites the stub
- Interrupted sync: pre-populated partial manifest causes only the remaining items to be synced
- Missing local file: tool prints an error, removes the tag, and continues (no stub, no crash)
- Module sync: module items created in the correct order; SubHeader items interleaved correctly
- **ExternalUrl module items**: absolute URLs produce `ExternalUrl` type items; `target="_blank"` HTML comment sets `new_tab: true`; missing comment defaults to `new_tab: false`; local and external links can coexist
- Timestamp skip: a file whose mtime is older than `last_synced` is skipped; `--force-uploads` overrides
- Quiz sync: quiz created/updated; questions deleted and re-created in order; skipped when all files up-to-date; re-synced when any question file is newer than `last_synced`; quiz module items use type `"Quiz"`
- **Assignment extended fields**: `lock_at`, `unlock_at`, `grading_type` from frontmatter reach `canvasapi` (fixture `assignments/week1.md` carries these fields)
- **Graded discussion fields**: `points_possible`, `due_at`, `lock_at`, `unlock_at` passed to `create_discussion_topic()` as `assignment={...}` nested dict (fixture `discussions/week1-intro.md` carries these fields)
- **Syllabus sync**: `course_settings/syllabus.md` body converted to HTML and passed to `course.update(course={"syllabus_body": ...})`; absent file → no update called
- **Course metadata sync**: flat fields from `course_settings/course_settings.toml` reach `course.update(course={...})`; absent file → no crash
- **Assignment group weights**: `group_weighting_scheme = "percent"` → `apply_assignment_group_weights: true` in the course update; any other scheme → `false`; scheme absent but a `group_weight` present on any `[[assignment_groups]]` entry → inferred `true`; neither present → flag not sent; explicit scheme wins over inference; full-pipeline test asserts both the flag and `group_weight` on `create_assignment_group()`
- **Rubric sync**: association by title and by numeric id; `use_for_grading` default and override; unknown title warns and skips; removal when the `rubric:` key is dropped; per-rubric hash caching skips unchanged rubrics. The course-level association must be created with `purpose: "bookmark"` — asserted directly, because `"grading"` makes Canvas fork the rubric on every edit instead of updating it (see ARCHITECTURE.md)
- **Rubric deleted on Canvas**: a two-run test where `get_rubrics()` stops listing a previously-synced rubric asserts the rubric is re-created *and* the referencing assignment is re-associated with the new id — while `create_assignment` is **not** called, since the point is that the assignment's own file is unchanged and never reaches `_apply_rubric`. Paired with tests that a still-present rubric triggers neither, and that a first sync never reports its rubrics as deleted (the empty-course case, which also covers `--check-all`). These tests build a minimal repo rather than using the shared fixture tree — the fixture's module-item mocks are one-shot iterators that a second `run_sync` exhausts
- **`course_settings/` folder isolation**: files inside `course_settings/` are never uploaded as Canvas Pages
- `--target-recursively`: BFS from a module reaches all referenced content; module deferred until content is in manifest
- `--single-target`: only specified files processed; no BFS
- Combined `-t` + `-s`: `-t` uploads first, `-s` skips anything already uploaded via `needs_sync`

## Layer 3 — Test fixtures (`tests/fixtures/`)

A minimal but complete course repo committed directly into this tool repo. It covers every case the tests need — cross-links between content types, snippet includes, nested assets, modules with SubHeaders, files with every frontmatter variant. `assignments/week1.md` includes `lock_at`, `unlock_at`, and `grading_type`. `discussions/week1-intro.md` includes `points_possible`, `due_at`, `lock_at`, and `unlock_at` for graded discussion testing. Fixtures are plain files; no special tooling needed to use them. `tests/fixtures/course_settings/course_settings.toml` contains only `format_version = 1`: every library entry point refuses a repo below the current format version, so the fixture repo needs one. Its side effect is that the settings phase now runs on the fixture and records a `course_settings/course_settings.toml` manifest entry, without any Canvas call.

**Repos built in `tmp_path`** need a current `course_settings/course_settings.toml` too. `tests/conftest.py` has `make_current(repo)`, which creates that file containing `format_version = FORMAT_VERSION`, or prepends the key to an existing file (and does nothing if the key is already there). Tests call it after writing their own settings file, since overwriting the file removes the key. Manifests written by hand in tests need a `_repo_format` entry (`manifest.flush()` adds one automatically).

The IMSCC fixture (`tests/fixtures/imscc/`) is a synthetic `.imscc`-style directory covering all content types. It includes:

- `imsmanifest.xml` — with lom lifecycle (last_modified) and rights (copyright) metadata
- `course_settings/course_settings.xml` — all course settings fields including booleans, ints, and nested `default_post_policy`
- `course_settings/grading_standards.xml` — one grading standard with threshold data array
- `course_settings/assignment_groups.xml` — two groups, one with drop-lowest rules
- `course_settings/late_policy.xml` — late submission deduction settings
- `course_settings/context.xml` — canvas_domain and course_id (used to pre-fill `canvas.toml`)
- `course_settings/events.xml` — two events (one all-day, one with HTML description)
- `course_settings/module_meta.xml` — one module with items of every supported type
- `course_settings/rubrics.xml` — two rubrics with criteria and ratings (tests extraction to `rubrics.toml`)
- `course_settings/files_meta.xml` — one hidden folder and three files with varied settings (tests extraction to `files_meta.toml`)
- `course_settings/media_tracks.xml` — empty (skipped; no round-trip content)
- `course_settings/canvas_export.txt` — export metadata text (skipped; no round-trip content)
- `course_settings/syllabus.html` — syllabus HTML content
- `wiki_content/`, `g_assignment_1/`, `g_discussion_1*.xml`, `g_quiz_1/`, `g_exturl_1.xml`, `web_resources/` — one of each content type
- `g_exturl_1.xml` — webLink with `target="_blank"` and `windowFeatures` on the `<url>` element (tests §4.8 attribute extraction and module comment output)
- `g_discussion_attach.xml` + `g_discussion_attach_meta.xml` — discussion with `<attachments>` block (tests §4.7 attachment extraction and `## Attachments` section)
- `g_quiz_1/g_quiz_1.xml` — updated with `<itemfeedback>` elements on MCQ (general, correct, incorrect, per-answer) and `<itemfeedback ident="solution">` on essay question (tests §4.10.7 feedback and §4.10.11.2 sample solution)
- `g_quiz_types.xml` — standalone QTI file with `cc.multiple_response.v0p1`, `cc.fib.v0p1`, `cc.pattern_match.v0p1` questions (unit tests for new question type parsing and output format)
- `lti_resource_links/g_lti_1.xml` — LTI 1.3 resource XML (tests that `imscc_path` is populated from `<file>` children, not from the empty `href=""` attribute)
- `g_quiz_2/` with `assessment_meta.xml` and `assessment_qti.xml` + `non_cc_assessments/g_quiz_2.xml.qti` — quiz in Canvas export format (tests the `href="" + <dependency>` detection path; also tests that `non_cc_assessments/*.xml.qti` is preferred for question parsing because it carries `points_possible`)
- `non_cc_assessments/g_bank_1.xml.qti` — QTI objectbank with 2 questions (MCQ with `original_answer_ids`, essay), `bank_context_uuid`, and `bank_state` (tests question bank category detection, metadata extraction, and `question_banks/` output)
- Module updated to include a `ContextExternalTool` item (tests that it is emitted as a URL link, not as a SKIPPED comment)

**What to assert on:** The integration tests assert on the `canvasapi` calls our code makes — arguments, order, and count. They do *not* assert on Canvas's behaviour (storing, retrieving), because that is Canvas's responsibility, not ours.

## Tools

- `pytest` — test runner
- `pytest-mock` — `canvasapi` mocking
- `tomllib` (stdlib) — manifest fixture parsing in tests
